#if !defined(_QUICK_SORT)
#define _QUICK_SORT

#include <iostream>
#include <vector>

int partition(std::vector<int>& vec, int low, int high);

void QuickSort(std::vector<int>& vec, int low, int high);

#endif _QUICK_SORT
